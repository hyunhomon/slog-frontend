import styled, { css } from "styled-components";

const WholeContainer = styled.div `
    width: 100%;
    height: 100%;

    position: absolute;
    right: 0;
`;

function ProfilePage() {
    return (
        <WholeContainer>profile</WholeContainer>
    );
};

export default ProfilePage;
