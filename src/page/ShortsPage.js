import styled, { css } from "styled-components";

const WholeContainer = styled.div `
    width: 100%;
    height: 100%;

    position: absolute;
    right: 0;
`;

function ShortsPage() {
    return (
        <WholeContainer>shorts</WholeContainer>
    );
};

export default ShortsPage;
